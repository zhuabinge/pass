<?php
class StatisticsModel extends BpfModel
{
  public function getStatisticsFileInfo()
  {
    // $fp = fopen(dirname(__FILE__) . '/statistics.txt', "r");
    // $result = fread($fp, filesize(dirname(__FILE__) . '/statistics.txt'));
    // fclose($fp);
    // return $result;
  }
}
